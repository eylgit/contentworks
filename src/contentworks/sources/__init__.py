"""Where source material comes from.

One module per kind of source. Each one returns the same thing — a list of
``Chapter`` objects — so the rest of the package never learns what format the
material arrived in.
"""
